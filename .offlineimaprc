[general]
# Use comma for separator
accounts        = Gmail
# Give it corrct permissions: chmod 755 ~/.offlineimap.py
pythonfile      = ~/.offlineimap.py
socktimeout     = 60
maxsyncaccounts = 3

[Account Gmail]
localrepository  = Gmail-Local
remoterepository = Gmail-Remote
status_backend   = sqlite
autorefresh      = 5
quick            = 10

[Repository Gmail-Local]
type            = Maildir
localfolders    = ~/Mail/Gmail

[Repository Gmail-Remote]
type            = Gmail
remoteport      = 993
remoteuser      = zlatozar@gmail.com
remotepasseval  = get_password_emacs("imap.gmail.com", "993")
ssl                = yes
sslcacertfile      = /etc/ssl/certs/ca-certificates.crt
holdconnectionopen = yes
maxconnections     = 3
folderfilter       = lambda foldername: foldername not in ['[Gmail]/Spam', '[Gmail]/All Mail', '[Gmail]/Starred', '[Gmail]/Important']
realdelete         = no
